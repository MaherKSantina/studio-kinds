{
  "name": "studio-kinds",
  "private": true,
  "version": "0.1.0",
  "description": "The Studio — one editor for every document kind: the crosscut kit, the file-kind layer, the web Studio, the desktop app, the VS Code extension. The checker is the Python package in python/ (studio-check).",
  "license": "MIT",
  "scripts": {
    "test": "pnpm -r --if-present test",
    "typecheck": "pnpm -r --if-present typecheck",
    "build": "pnpm -r --if-present build",
    "desktop:renderer": "pnpm --filter studio-frontend build:desktop",
    "desktop": "pnpm --filter studio-frontend build:desktop && pnpm --filter studio-desktop start",
    "vscode:renderer": "pnpm --filter studio-frontend build:vscode",
    "vscode:vsix": "pnpm --filter studio-frontend build:vscode && pnpm --filter studio-vscode package",
    "skill:export": "python scripts/export_skill.py",
    "desktop:dist": "pnpm --filter studio-frontend build:desktop && pnpm --filter studio-desktop dist",
    "release:build": "pnpm vscode:vsix && pnpm desktop:dist",
    "kinds:check": "python scripts/kind_books.py --check",
    "kinds:readme": "node scripts/book-to-markdown.mjs",
    "kinds:readme:check": "node scripts/book-to-markdown.mjs --check"
  },
  "engines": {
    "node": ">=20"
  }
}
