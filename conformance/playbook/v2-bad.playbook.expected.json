{
  "ok": false,
  "problems": [
    "`rules:` — at version 2 an event carries its own `when` and `hint`, and its document shows once the answers it follows are taken. Move each rule's `process` onto its event as `hint`, a `when` where the event is on the table only under an answer, or take `version: 2` off",
    "`topics:` — at version 2 what is always true is the content of an always-on event, or take `version: 2` off",
    "`view:` — a version-2 walk is session-only and writes nothing; every decision opens unanswered. Delete the view",
    "event rules-here: `status` — at version 2 the document shows when its answers are taken, and `hint` shows until then. Delete it",
    "event rules-here: `sets` — at version 2 an answer is taken on the rail, never by an event. Delete it",
    "event rules-here: `content` is one document at version 2 — a mapping, not a list; several sections belong in one brief",
    "event file-entry: Beside is a file beside the book — at version 2 the document is in the book; write it as `kind` + `doc`, or take `version: 2` off",
    "event neither: Nothing has neither `doc` nor `docs` — write the document under it",
    "event by-unknown: `by` names zz, which is not a decision of this book",
    "event no-kind: inline content has `doc` but no `kind` — say which kind renders it (brief, guide, md, …)",
    "event both: inline content has both `doc` and `docs` — one document, or one per answer combination",
    "event both: inline content is one document, so it cannot vary `by` — write one per answer combination under `docs`",
    "event both: inline content: docs has no `d=b` — every answer combination of `by` is a document of its own",
    "event doc-by: inline content is one document, so it cannot vary `by` — write one per answer combination under `docs`",
    "event docs-no-by: inline content has `docs` but no `by` — say which decisions key the documents",
    "event docs-bad-keys: inline content: docs has no `d=b,e=x` — every answer combination of `by` is a document of its own",
    "event docs-bad-keys: inline content: docs has `d=c,e=x`, which is not a combination of d, e answers in `by` order",
    "event docs-bad-keys: inline content: docs has `e=x,d=b`, which is not a combination of d, e answers in `by` order",
    "event md-not-string: inline content: an `md` document is its text — write it as a block string",
    "event unknown-kind: inline content (widget): not a kind the Studio knows — `--kinds` lists them",
    "event nested-broken: inline content (brief) [e=x]: section S (kanban): `columns` is empty — a board needs at least one status",
    "event nested-broken: inline content (brief) [e=x]: section S (kanban): task no key: no `key` — the stable key `needs` edges point at"
  ],
  "summary": "v2 · 2 decisions, 12 events, 9 documents"
}
